'use strict';

// ==================================================
// Styles configuration
// ==================================================

module.exports = {
  enabled: true,
  autoprefixer: ['last 2 versions'],
  maps: true,
  minify: true
};
